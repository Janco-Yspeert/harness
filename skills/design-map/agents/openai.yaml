interface:
  display_name: "Design Map"
  short_description: "Record settled spike design decisions"
  default_prompt:
    "Use $design-map to create the compact structural contract for a frozen
    Harness spike."
