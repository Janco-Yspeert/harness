interface:
  display_name: "Spike Review"
  short_description: "Review a spike brief for freeze readiness"
  default_prompt:
    "Use $spike-review to review a spike brief against the current repository
    and decide whether it is ready to freeze."
