# Spike 005 App Server Protocol Baseline

Codex CLI: codex-cli 0.147.0

The App Server JSON Schema in `app-server-schema/` was generated from this Codex
CLI installation for Spike 005.

Spike 005 targets the non-experimental App Server protocol surface and does not
opt into `capabilities.experimentalApi`.
