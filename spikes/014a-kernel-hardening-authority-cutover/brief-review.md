# Brief Readiness — Spike 014a Kernel Hardening: Bounded Continuation, Supersession, and Authority Cutover

## Verdict

The brief is ready to govern independent Design Map, implementation, and evaluation work.

## Source and inherited-contract coverage

- The supplied orchestration assignment requires a successor—not a retroactive
  correction cycle—for Spike 014, and identifies the three inherited defects
  plus the evaluator-coverage gap.  The brief preserves precisely that
  distinction in its status, Part I, and Part II.
- `spikes/014-kernel-consolidation-authority-role-grants/acceptance.md` records
  a genuine evaluator `PASS`, completed promotion and As-Built, followed by a
  human rejection for evaluator-result vocabulary/cross-field drift,
  non-equivalent allocation deduplication, silent continuation failure, and
  evaluator coverage.  Part I requires the corresponding repairs without
  rewriting the predecessor result.
- The predecessor As-Built records the configured generic kernel, canonical
  ledger, pinned methodology, attached/spawned execution, exposure provenance,
  separate semantic/process/host-action facts, and host-mediated actions.  The
  brief explicitly preserves those boundaries and limits the successor to
  hardening/cutover rather than a redesign.
- The active evaluator skill is contract version 11 and names
  `IMPLEMENTATION_FAILURE`, `EVALUATOR_DEFECT`,
  `SPECIFICATION_AMBIGUITY`, `SPECIFICATION_DRIFT`, and
  `INFRASTRUCTURE_FAILURE`.  The current configured evaluator-verify contract
  instead includes `SPECIFICATION_DEFECT`; the brief accurately treats this as
  inherited fidelity work and prohibits evaluator-specific kernel conditionals.
- The supplied assignment's new successor requirements—finite automatic-work
  authority, one active governed lease, explicit supersession, inline
  adoption, promotion through a configured host action, governed human
  acceptance/rejection, and deterministic `after` semantics—are explicitly
  marked new Part II scope.  Acceptance criteria, regression scenarios, real
  boundaries, design freedom, and non-goals give them a fair, bounded contract.

## Material findings

None.  The brief leaves implementation choices that do not change the required
observable behavior (for example, budget representation and the generic
host-action shape) to the Design Map, while fixing the semantics that must not
vary.  It defines supersession ordering, post-supersession rejection, budget
exhaustion, promotion/PASS separation, and the missing-anchor predicate result
well enough for independent evaluation.

## Readiness strengths

- It separates inherited defects from newly discovered successor scope rather
  than laundering later requirements into a false claim about the frozen Spike
  014 contract.
- It protects the important negative boundaries: no local-state authority, no
  generic-kernel role vocabulary, no concurrent governed role executions, and
  no direct executor promotion authority.
- It requires both implementation-independent negative cases and three
  supported real-boundary proofs, without prescribing an unnecessary redesign.

## Evidence inspected and limitations

Inspected the supplied orchestration assignment; `AGENTS.md`; `GOALS.md`; the
complete 014a `spike.md`; the predecessor `spike.md`, `acceptance.md`, and
`as-built.md`; the active evaluator-v11 skill; the current Harness policy and
evaluator-verify contract; relevant kernel allocation, continuation, and
predicate code; and the predecessor authority status.  `npm run workflow --
authority status spikes/014-kernel-consolidation-authority-role-grants`
recognizes the `human-rejected` event as a closed cycle with technical
verification `PASS`, completed promotion/As-Built, and successor permission.
The legacy authority command has no whole-ledger `validate` invocation; its
status path reconstructs and validates the history, while transition validation
is request-specific.  No current-spike later artifacts or evaluator-private
paths were inspected.

READY
