# Criterion evidence records - evaluator revision 001

One record per material frozen acceptance criterion (`spike.md` AC01-AC21). The
frozen-authority source for every record is the numbered acceptance criterion in
`spike.md` (`sha256:ba7f7c0a...0455`, frozen at `9af63ce`), read together with
the cited frozen `spike.md` scope section and the frozen Design Map
(`sha256:22f01566...0a97b`, frozen at `3689964`). Each record adds the
criterion-specific inference that its referenced evidence is sufficient.
Procedures shared across records (E1, E2, E5, E7, E10, S1, S2, S3, S4, P1) each
state their own reason rather than delegating to the group.

| ID   | Frozen authority                                                     | Evidence mode         | Procedures            | Required | Criterion-specific sufficiency reason                                                                                                                                                                                             |
| ---- | ------------------------------------------------------------------- | --------------------- | --------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AC01 | spike.md AC01; scope 1, 1A, 6; DM "Shared contracts", "Design decisions" | COMPOSITE          | E1, S1               | required | E1 shows a run requested through the supported path outliving its initiating client under host ownership; S1 confirms the registry and its lifecycle are created and owned by the existing Harness host, not a new daemon or parallel control plane. Together they establish host ownership behaviourally and structurally. |
| AC02 | spike.md AC02; scope 1; DM invariant 1                              | PUBLIC_REGRESSION     | E1                   | required | AC02 is the client-detachment claim; E1 disconnects the initiating client after launch and shows the Harness-owned run continues and is not lost while the host is alive - the direct falsification test.                          |
| AC03 | spike.md AC03; scope 1; DM "Shared contracts"                       | PUBLIC_REGRESSION     | E1                   | required | AC03 is the reconnect-and-inspect claim; E1 has a later client resolve and inspect the same active or terminal run by its stable Harness run identity.                                                                             |
| AC04 | spike.md AC04; scope 2; DM "Design decisions"                       | PUBLIC_REGRESSION     | E2                   | required | AC04 enumerates the run-record contents; E2 asserts the canonical run record exposes each listed field (slot, execution attempt, role, executor, invocation mode, workspace, permission profile, lifecycle, timestamps, process/provider identity) in a stable observable form, and does not invent unavailable metadata. |
| AC05 | spike.md AC05; scope 3; DM "Shared contracts"                       | PUBLIC_REGRESSION     | E3                   | required | AC05 requires the methodology attempt and the execution attempt to be distinct and observable; E3 replaces a worker, creating a new execution attempt for the slot without a new methodology attempt, and shows both identities separately observable. |
| AC06 | spike.md AC06; scope 4; DM "Shared contracts"                       | PUBLIC_REGRESSION     | E4                   | required | AC06 is the single-active-execution claim; E4 issues two concurrent/repeated start requests for one active slot and shows at most one child/provider execution, with a concurrency-safe check and allocation.                      |
| AC07 | spike.md AC07; scope 4; DM "Implementation freedom"                 | PUBLIC_REGRESSION     | E4                   | required | AC07 constrains the duplicate-request response; E4's decision rule requires the duplicate to return/identify the existing run or fail with an explicit conflict, never a second worker - the `200`-or-conflict choice itself is Design Map freedom. |
| AC08 | spike.md AC08; scope 5, "Failure and recovery semantics"; DM "Shared contracts" | PUBLIC_REGRESSION | E5              | required | AC08 is the replacement-ordering precondition; E5 shows a replacement cannot be allocated while the prior canonical execution for the slot is still active.                                                                        |
| AC09 | spike.md AC09; scope 5; DM "Shared contracts", invariant 4          | PUBLIC_REGRESSION     | E5                   | required | AC09 requires the replacement record to preserve prior identity, new identity, reason, and executor change; E5 allocates a replacement and asserts each of those is recorded, including the fallback-executor case.                 |
| AC10 | spike.md AC10; scope 6, 1A; DM "Shared contracts", "Design decisions" | COMPOSITE           | E6, S1, S3          | required | E6 shows the canonical `tools/workflow.ts` path requesting/attaching to a host-owned run and failing explicitly when no host is reachable; S3 confirms it no longer directly owns detached child processes; S1 confirms the run mechanism is the existing host, not a parallel methodology daemon. |
| AC11 | spike.md AC11; scope 6; DM "Design decisions"                       | PUBLIC_REGRESSION     | E7                   | required | AC11 is the terminal-completion-binding claim; E7 shows the runner refusing to record a phase complete unless the canonical run for the matching phase/attempt is terminally `completed`.                                          |
| AC12 | spike.md AC12; scope 7; DM "Design decisions"                       | COMPOSITE            | E8, S4               | required | E8 shows structured events distinguishing allocated, started, activity, completed, failed, cancelled, and replaced; S4 confirms those events reuse the existing Harness structured-event protocol rather than a second event system. |
| AC13 | spike.md AC13; scope 8; DM "Design decisions"                       | COMPOSITE            | E9, S4               | required | E9 shows execution text/logs inspectable and tied to the run but not a frozen methodology artifact, downstream context, or completion signal; S4 confirms the diagnostic-log handling structurally keeps that separation.          |
| AC14 | spike.md AC14; scope 9; DM invariant 5                              | PUBLIC_REGRESSION     | E10                  | required | AC14 enumerates the routine operations a standard worker must perform without per-command prompts; E10 exercises repository reads/writes, local computation including shell child processes, tests/checks, and role-permitted Git within the allowed workspace with no human prompt. |
| AC15 | spike.md AC15; scope 9; DM invariant 5                              | COMPOSITE            | E10, S2              | required | E10 shows an evaluator run declaring an additional private evaluator workspace; S2 confirms that profile differs from the standard one only by adding that declared workspace and grants no arbitrary unrestricted host access.     |
| AC16 | spike.md AC16; scope 10; DM "Design decisions", invariant 5         | COMPOSITE           | E10, S2              | required | S2 confirms the default local backend uses an explicitly named workspace-bounded profile and not an unrestricted-bypass mode, with any bootstrap exception explicit in the Design Map/execution record; E10 shows routine work completing without that mode. |
| AC17 | spike.md AC17; scope 9; DM "Design decisions"                       | COMPOSITE            | E2, E10, S2          | required | E2 asserts the permission-profile identity is a field on the run record; E10 shows it recorded for a concrete run; S2 confirms the recorded value names the applied bounded profile.                                               |
| AC18 | spike.md AC18; scope 11; DM invariant 3                             | PUBLIC_REGRESSION     | E11                  | required | AC18 enumerates the cheap run metrics; E11 asserts the run records elapsed time, executor, execution attempt/replacement count, and terminal disposition from directly observable facts, omitting rather than estimating unavailable provider usage. |
| AC19 | spike.md AC19; scope 6; DM "Design decisions"                       | COMPOSITE            | E7, S3               | required | E7 shows a directly launched process with no canonical run identity failing to satisfy the runner's completion requirement despite plausible artifacts; S3 confirms a direct external process has no usable run identity for the slot. |
| AC20 | spike.md AC20; scope 1A, 6, 7; DM "Design decisions"               | COMPOSITE            | E1, E4, E5, E7, E8, E9, E12, P1 | required | E1/E4/E5/E7/E8/E9 are the enumerated behaviours (client detachment, duplicate-launch prevention, replacement ordering, terminal-completion binding, event visibility, diagnostic-log/noncanonical separation); E12 shows them driven through an in-memory backend seam with no live paid provider call; P1 confirms the visible suite runs green. |
| AC21 | spike.md AC21                                                       | PROVENANCE_INSPECTION | P1                   | required | AC21 is a set of command exit codes plus continued green pre-existing suites at the evaluated commit; P1 records `npm test`, typecheck, lint, `format:check`, `git diff --check`, and the session/backend suites.                  |

No new acceptance semantics are introduced. S1-S4 inspect the frozen
implementation source at the evaluated commit against concrete decision rules,
E1-E12 exercise only the implementation's own visible regression suite through
`npm test`, and P1-P2 are command-exit and `git` provenance checks. Negative
requirement N8 (no rewrite of Spikes 008-010c) is established by P2 and is not a
numbered acceptance criterion, so it carries no `coverage-map.json` record.
